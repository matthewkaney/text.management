name: Build Desktop Apps

on:
  workflow_dispatch:
  push:
    tags:
        - "v*.*.*"

jobs:
  create-release:
    name: Set up Release
    runs-on: ubuntu-latest
    
    steps:
      - uses: softprops/action-gh-release@v1
        with:
          tag_name: v0.6.0
          name: Test Release
          draft: true
        
  build-app:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
        include:
          - os: ubuntu-latest
            name: Linux
            files: out/make/deb/x64/*.deb
          - os: macos-latest
            name: Mac OS
            files: out/make/zip/darwin/x64/*.zip
          - os: windows-latest
            name: Windows
            files: out\make\squirrel.windows\x64\*.exe
        
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3.5.1
      with:
         node-version: 18
         cache: npm
    - run: npm ci
    - name: Build Packages
      run: npm run prepare-old
    - name: Build Desktop App
      run: npm run build:desktop
    - name: Make Desktop App
      run: npx electron-forge make app/desktop
    - name: Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v0.6.0
        files: ${{ matrix.files }}
